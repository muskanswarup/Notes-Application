<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="main w-full min-h-screen bg-zinc-900 text-white">
        <h1 class="text-5xl p-5 bg-gradient-to-r from-blue-600 via-green-500 to-indigo-400 inline-block text-transparent bg-clip-text ml-5">Notes Taking Application</h1>
        <div class="form p-10">
            <form action="/create" method="post" >
                <input type="text" name="title" class="block px-5 py-2 bg-zinc-700 w-full rounded-lg" placeholder="Title goes here...">
                <textarea name="details" class="block px-5 py-2 mt-3 resize-none bg-zinc-700 w-full rounded-lg"  placeholder="Write your details here..."></textarea>
                <button type="submit" class="bg-blue-600 px-5 rounded-lg py-2 mt-2 hover:bg-blue-700">Create task!</button>
            </form>
        </div>
        
        <div class="tasks p-10 flex gap-x-4 flex-wrap">

            <% if(files.length > 0){ %>
            <% files.forEach(function(val){ %>
                    <div class="task w-72 px-3 py-4  bg-zinc-600 rounded-lg text-xl mb-2 ">
                        <h1 class="text-lime-200 p-2"> <%= val %> </h1>
                        
                        <div class="flex w-full justify-between items-center">
                            <button class="bg-blue-600 px-5 rounded-lg py-2 mt-2 mr-2 hover:bg-blue-700">
                                <a href="/files/<%= val %>" class="inline-block">Read more</a>
                            </button>
    
                            <button class="bg-blue-600 px-5 rounded-lg py-2 mt-2 hover:bg-blue-700">
                                <a href="/edit/<%= val%>">Edit</a>
                            </button>
                        </div>
                        
                       
                    </div>
            <% }) %>
            <% } else{    %>
                <h3 class="text-zinc-600">No tasks yet.</h3>
            <% } %>

           

            <div class="task w-72 px-3 py-4 hidden bg-zinc-600 rounded-lg text-2xl">
                <h1>Some task goes here.</h1>
                <a href="" class="text-blue-500 inline-block mt-2">Read more.</a>
            </div>

            <div class="task w-72 px-3 py-4 hidden bg-zinc-600 rounded-lg text-2xl">
                <h1>Some task goes here.</h1>
                <a href="" class="text-blue-500 inline-block mt-2">Read more.</a>
            </div>

            <div class="task w-72 px-3 py-4 hidden bg-zinc-600 rounded-lg text-2xl">
                <h1>Some task goes here.</h1>
                <a href="" class="text-blue-500 inline-block mt-2">Read more.</a>
            </div>
        </div>
    </div>
</body>
</html>